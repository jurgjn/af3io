[project]
name = "af3io"
version = "0.1"
description = "Utilities for AlphaFold 3 input & predictions"
readme = "README.md"
authors = [
    { name = "jurgjn", email = "jurgjn@users.noreply.github.com" }
]
requires-python = ">=3.10"
dependencies = [
    "click",
    "humanfriendly",
]

[project.scripts]
af3io = "af3io.cli:cli"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
